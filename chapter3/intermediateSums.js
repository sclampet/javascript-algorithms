function intermediateSums(arr) {
  let total = 0;

  for(var i = 0; i < arr.length; i++) {
    total += arr[i];
    if(i % 10 === 0 || i === arr.length - 1) {
      
    }
  }
};

module.exports = {
  intermediateSums
};